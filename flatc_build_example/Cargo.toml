[package]
name = "flatc-build"
version = "0.1.0"
authors = ["Toshiki Teramura <toshiki.teramura@gmail.com>"]
edition = "2018"

[dependencies.flatbuffers]
path = ".."

[build-dependencies]
cmake = "0.1"
